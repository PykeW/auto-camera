# 相机控制界面 - UI结构与元素总结 (供HTML/CSS重构参考)

## 1. 整体布局 (Overall Layout)

页面采用上下左右结构，主要分为三个区域：

1.  **顶部标题栏 (`.header`)**: 固定在顶部，包含控制按钮和标题。
2.  **主内容区 (`.main-content`)**: 占据中间大部分区域，左右分栏：
    *   **左侧相机预览区 (`.camera-view`)**: 显示相机画面（目前用灰色背景代替）。
    *   **右侧控制面板 (`.control-panel`)**: 包含各种设置和操作。
3.  **底部状态栏 (`.status-bar`)**: 固定在底部，显示状态信息。

**关键布局样式**:
*   整体使用 `display: flex; flex-direction: column;`
*   主内容区使用 `display: flex;`
*   控制面板固定宽度（例如 `300px`），预览区占据剩余空间 (`flex: 1;`)。

## 2. 顶部标题栏 (`.header`)

*   **左侧**: 一组控制按钮 (`.header-controls > .header-button`)，使用特殊字符或图标：
    *   `«` (Button)
    *   `⊡` (Button)
    *   `▶` (Button)
    *   `▷` (Button)
    *   `CameraOutlined` (Icon Button)
*   **右侧**: 标题文本 (`.header-title`)："相机操作"

**关键样式**:
*   深色背景 (`#2d2d2d`)。
*   按钮为文本样式，白色图标/文字。
*   固定高度（例如 `40px`）。

## 3. 主内容区 (`.main-content`)

### 3.1. 相机预览区 (`.camera-view`)

*   主要用于显示图像（当前为占位符）。
*   包含一个绝对定位的叠加层 (`.focus-overlay`)，用于显示对焦ROI的虚线框（样式由JS动态添加）。

**关键样式**:
*   灰色背景 (`#666`) 或用于显示图像的元素。
*   `position: relative;` 以便 `.focus-overlay` 定位。
*   `.focus-overlay` 初始无样式或隐藏，JS根据需要添加 `border`, `left`, `top`, `width`, `height`。

### 3.2. 控制面板 (`.control-panel`)

使用标签页 (`.control-tabs`) 组织内容。

**关键样式**:
*   深色背景 (`#2d2d2d`)，内边距 (`padding`)。
*   白色文字。
*   标签页本身有特定样式（选中/未选中状态）。
*   内容超出时可滚动 (`overflow-y: auto;`)。

#### 3.2.1. 标签页一：基本设置 (`TabPane key="1"`)

包含多个设置区域 (`.panel-section`)：

*   **相机序列号**:
    *   标题 (`.section-header`)
    *   连接按钮 (`<button class="connect-button">连接</button>`)
*   **配置文件**:
    *   标题 (`.section-header`)
    *   选择文件按钮 (`<button class="config-button">选择文件</button>`)
*   **图像保存路径**:
    *   标题 (`.section-header`)
    *   只读输入框 (`<input type="text" readonly value="D:/AD1线下方验证">`)
    *   选择文件夹按钮 (`<button>选择文件夹</button>`)
*   **相机名称**:
    *   标题 (`.section-header`)
    *   输入框 (`<input type="text" value="新建相机1">`)
*   **相机型号**:
    *   标题 (`.section-header`)
    *   输入框 (`<input type="text" value="海康（面阵）">`)

#### 3.2.2. 标签页二：相机属性 (`TabPane key="2"`)

*   包含一个属性表格 (`<table class="property-table">`)：

| 属性名称             | 属性值 (控件类型)                     |
| :------------------- | :------------------------------------ |
| Y反转                | 下拉选择 (`<select>`) (是/否)         |
| X反转                | 下拉选择 (`<select>`) (是/否)         |
| 触发模式             | 下拉选择 (`<select>`) (连续/外部)     |
| 触发信号             | 下拉选择 (`<select>`) (上升/下降)     |
| 触发源               | 下拉选择 (`<select>`) (通道0/通道1)   |
| 图像格式             | 下拉选择 (`<select>`) (MONO8/RGB8)    |
| 曝光时间(微秒/us)    | 输入框 (`<input type="number">`)      |
| 增益                 | 输入框 (`<input type="number">`)      |
| 使能                 | 输入框 (`<input type="text">`)        |
| 伽马指数             | 输入框 (`<input type="number">`)      |
| 白平衡               | 输入框 (`<input type="text">`) (或Select) |
| 红(0)                | 滑块 (`<input type="range">` 或模拟)  |
| 绿(0)                | 滑块 (`<input type="range">` 或模拟)  |
| 蓝(0)                | 滑块 (`<input type="range">` 或模拟)  |

**关键样式**:
*   表格边框、单元格内边距。
*   表头行可能有不同背景色。

#### 3.2.3. 标签页三：自动对焦 (`TabPane key="3"`)

包含两个主要区域 (`.panel-section`)：

*   **对焦控制**:
    *   标题 (`.section-header`)
    *   **模式选择**: 下拉框 (`<select class="focus-mode">`) (手动/自动/半自动)
    *   **参数区域 (`.focus-params`)**:
        *   对焦步长(mm): 标签 + 数字输入框 (`<input type="number">`)
        *   扫描范围(mm): 标签 + 两个数字输入框 (最小/最大)
        *   当前位置(mm): 标签 + 只读输入框 (`<input type="text" readonly>`)
    *   **控制按钮 (`.focus-controls`)**:
        *   开始对焦: 按钮 (`<button type="primary">`)，含图标，有加载/禁用状态
        *   停止: 按钮 (`<button class="danger">`)，有禁用状态
        *   保存参数: 按钮 (`<button>`)，有禁用状态
    *   **状态显示 (`.focus-status`)**:
        *   对焦状态: 标签 + 状态文本 (`<span class="status-text status-...">`) - 文本和颜色会变
        *   清晰度值: 标签 + 数值文本 (`<span class="status-value">`)
        *   最佳位置: 标签 + 数值文本 (`<span class="status-value">`)

*   **对焦ROI设置**:
    *   标题 (`.section-header`)
    *   **设置按钮**: 按钮 (`<button>模拟设置对焦区域</button>`)，有禁用状态
    *   **坐标显示 (`.roi-coords`)**:
        *   左上角: 标签 + 两个只读输入框 (X/Y)
        *   右下角: 标签 + 两个只读输入框 (X/Y)

**关键样式**:
*   参数区、状态区、坐标区可能有独立背景色和内边距。
*   按钮间距。
*   状态文本根据状态（就绪、对焦中、已对焦、错误、停止）改变颜色。

#### 3.2.4. 其他设置 (原位于面板底部，后移入或隐藏)

*   **ROI区域**:
    *   标题
    *   启用按钮
    *   坐标输入 (左X, 上Y, 右X, 下Y)
    *   保存ROI按钮
*   **曝光时间**:
    *   标题
    *   标签 "(毫秒/ms)"
    *   输入框

## 4. 底部状态栏 (`.status-bar`)

*   包含一行状态信息文本，例如：
    *   "未连接"
    *   "帧率: 0 fps"
    *   "x=..., y=..."
    *   "位置超过图像范围"
    *   "图像: Pixmap(w, h)"
    *   "缩放: 100%"

**关键样式**:
*   深色背景 (`#2d2d2d`)。
*   白色文字，较小字号。
*   固定高度（例如 `24px`）。
*   使用 `display: flex;` 和 `gap` 或 `margin` 控制间距。

## 5. 通用控件样式 (覆盖默认或Ant Design)

*   **输入框 (`<input>`, `<select>`)**: 深色背景 (`#3d3d3d`)，深色边框 (`#444`)，白色文字。
*   **按钮 (`<button>`)**: 深色背景 (`#3d3d3d`)，深色边框 (`#444`)，白色文字。
    *   **主按钮 (Primary)**: 蓝色背景 (`#1890ff`)。
    *   **危险按钮 (Danger)**: 红色背景 (`#ff4d4f`)。
*   **滑块 (`<input type="range">` 或模拟)**: 自定义轨道 (`#444`) 和滑块 (`#1890ff`) 颜色。

---

这个总结应该能为您提供一个清晰的蓝图，说明需要创建哪些HTML元素以及需要应用哪些关键的CSS样式来复现之前的React界面。祝您重构顺利！
