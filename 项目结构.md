.
├── .eslintrc.js
├── .gitignore
├── index.html
├── package.json
├── package-lock.json
├── vite.config.js
├── 项目结构.md
├── public/
│   └── favicon.png
└── src/
    ├── App.vue
    ├── main.js
    ├── assets/
    │   └── styles.css
    ├── components/
    │   ├── AppHeader.vue
    │   ├── StatusBar.vue
    │   ├── CameraView.vue
    │   ├── common/
    │   │   ├── AxisControl.vue
    │   │   ├── FocusThumbnails.vue
    │   │   └── RoiOverlay.vue
    │   └── ControlPanel/
    │       ├── CalibrationControl.vue
    │       ├── CameraConnection.vue
    │       ├── FocusControl.vue
    │       ├── index.vue
    │       ├── CameraProperties.vue
    │       ├── CameraInfo.vue
    │       └── ROIControl.vue
    ├── stores/
    │   ├── axis.js
    │   ├── calibration.js
    │   ├── focus.js
    │   ├── camera.js
    │   └── roi.js
    └── utils/
        ├── imageGenerator.js
        └── helpers.js

## 相机标定部分

### ROI绘制功能

相机标定模块中的ROI绘制功能已完善，现在具有以下特性：

1. **实时ROI信息显示**：在绘制ROI过程中，用户可以实时看到ROI的位置和尺寸信息，包括左上角坐标(L,T)和宽高(W,H)。
2. **实时ROI预览**：绘制过程中ROI区域会实时显示，不需要等待绘制完成。
3. **多种ROI形状支持**：
   - 矩形：通过拖拽确定对角线位置
   - 多边形：通过点击确定多个顶点，双击完成绘制
   - 椭圆：通过拖拽确定椭圆的大小和位置

### 标定流程简化

标定流程已简化，删除了以下步骤：
1. ~~检测mark点~~：不再需要单独的检测mark点步骤
2. ~~居中mark点~~：不再需要居中mark点的步骤

新的标定流程：
1. 选择X轴和Y轴（必选）和U轴（可选）
2. 绘制ROI区域，确定标定范围
3. 通过截取ROI区域创建模板图片
4. 直接点击"开始标定"按钮，系统将在ROI区域内进行标定
5. 标定过程中将依次显示9张标定图片，并自动在每张图片上匹配模板位置
6. 标定过程中可以随时点击"停止标定"按钮中断标定

### 9点图像标定功能

标定过程中的9点图像显示功能：
1. 开始标定后，系统将自动加载并依次显示9张标定图片
2. 每张图片显示时，系统将自动执行模板匹配，并在图片上框出匹配结果
3. 匹配框的颜色表示匹配度：
   - 绿色：高匹配度 (>0.9)
   - 黄色：中等匹配度 (0.7-0.9)
   - 橙色：低匹配度 (<0.7)
4. 每个匹配框上方显示匹配分数

### 模板匹配功能

模板匹配功能保留并增强：
1. 可以通过ROI截取模板图片
2. 可以在相机画面上绘制/隐藏模板，实时查看匹配效果
3. 可以调整匹配阈值，提高匹配精度
4. 标定过程中自动使用模板进行特征点匹配