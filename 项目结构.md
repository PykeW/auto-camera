# 项目结构说明

## 目录结构

```
auto-camera/
├── .cursor/                 # Cursor编辑器配置
├── public/                  # 静态资源
│   └── 9dian/               # 9点标定相关图片
├── src/                     # 源代码
│   ├── assets/              # 资源文件
│   │   ├── images/          # 图片资源
│   │   └── styles/          # 样式文件
│   │       └── main.css     # 主样式文件
│   ├── components/          # 组件
│   │   ├── Layout/          # 布局组件
│   │   │   ├── AppHeader.vue  # 应用头部
│   │   │   └── StatusBar.vue  # 状态栏
│   │   ├── CalibrationPanel/  # 标定面板组件
│   │   │   ├── CalibrationControl.vue        # 标定控制主组件
│   │   │   ├── AxisSelector.vue              # 轴选择器
│   │   │   ├── AxisParameters.vue            # 轴参数
│   │   │   ├── MatrixConfig.vue              # 矩阵配置
│   │   │   ├── MarkPointControl.vue          # 标记点控制
│   │   │   ├── CalibrationButtons.vue        # 标定按钮
│   │   │   └── CalibrationRatioCalculator.vue # 标定比率计算器
│   │   ├── FocusPanel/      # 对焦面板组件
│   │   │   ├── FocusControl.vue  # 对焦控制
│   │   │   └── ZAxisSelector.vue # Z轴选择器
│   │   ├── CameraPanel/     # 相机面板组件
│   │   │   ├── CameraView.vue       # 相机视图
│   │   │   ├── CameraInfo.vue       # 相机信息
│   │   │   ├── CameraConnection.vue # 相机连接
│   │   │   ├── CameraProperties.vue # 相机属性
│   │   │   └── ROIControl.vue       # ROI控制
│   │   ├── ui/              # UI组件
│   │   │   ├── AxisPositionControl.vue # 轴位置控制
│   │   │   ├── AxisSpeedControl.vue    # 轴速度控制
│   │   │   ├── AxisControl.vue         # 轴控制
│   │   │   ├── IconButton.vue          # 图标按钮
│   │   │   └── ActionButton.vue        # 操作按钮
│   │   └── common/          # 通用组件
│   │       ├── LabeledInputRow.vue     # 带标签的输入行
│   │       ├── SelectControl.vue       # 选择控件
│   │       ├── ValueInputControl.vue   # 值输入控件
│   │       ├── SimpleInputControl.vue  # 简单输入控件
│   │       ├── RoiOverlay.vue          # ROI覆盖层
│   │       ├── RoiToolsPanel.vue       # ROI工具面板
│   │       ├── RoiControlHeader.vue    # ROI控制头部
│   │       └── FocusThumbnails.vue     # 对焦缩略图
│   ├── stores/              # 状态管理
│   │   ├── camera.js        # 相机状态
│   │   ├── focus.js         # 对焦状态
│   │   ├── calibration.js   # 标定状态
│   │   ├── axis.js          # 轴状态
│   │   └── roi.js           # ROI状态
│   ├── utils/               # 工具函数
│   │   ├── helpers.js       # 通用辅助函数
│   │   ├── inputHelpers.js  # 输入辅助函数
│   │   └── imageGenerator.js # 图像生成器
│   ├── App.vue              # 根组件
│   └── main.js              # 入口文件
├── .eslintrc.js             # ESLint配置
├── .gitignore               # Git忽略文件
├── index.html               # HTML入口
├── package.json             # 项目配置
├── package-lock.json        # 依赖锁定
└── vite.config.js           # Vite配置
```

## 组件结构说明

### 布局组件 (Layout)
- **AppHeader.vue**: 应用头部组件，包含应用标题和主要操作按钮
- **StatusBar.vue**: 应用底部状态栏，显示相机状态和图像信息

### 标定面板组件 (CalibrationPanel)
- **CalibrationControl.vue**: 标定控制主组件，组织其他标定相关组件
- **AxisSelector.vue**: 轴选择器，用于选择X/Y/U轴
- **AxisParameters.vue**: 轴参数配置，包括速度和步进设置
- **MatrixConfig.vue**: 矩阵配置，设置标定矩阵大小和点位偏移
- **MarkPointControl.vue**: 标记点控制，包括模板匹配和轮廓提取
- **CalibrationButtons.vue**: 标定操作按钮，如开始/停止标定和保存矩阵
- **CalibrationRatioCalculator.vue**: 当量计算器，计算像素与实际尺寸的比例

### 对焦面板组件 (FocusPanel)
- **FocusControl.vue**: 对焦控制组件，管理自动对焦和手动对焦
- **ZAxisSelector.vue**: Z轴选择器，用于选择对焦轴

### 相机面板组件 (CameraPanel)
- **CameraView.vue**: 相机图像显示组件
- **CameraInfo.vue**: 相机信息显示组件
- **CameraConnection.vue**: 相机连接控制组件
- **CameraProperties.vue**: 相机属性设置组件
- **ROIControl.vue**: ROI(感兴趣区域)控制组件

### UI组件 (ui)
这些组件是通用UI元素，被其他功能组件使用：
- **AxisPositionControl.vue**: 轴位置控制组件
- **AxisSpeedControl.vue**: 轴速度控制组件
- **AxisControl.vue**: 综合轴控制组件
- **IconButton.vue**: 图标按钮组件
- **ActionButton.vue**: 操作按钮组件

### 通用组件 (common)
这些组件是通用功能组件，被多个功能模块使用：
- **LabeledInputRow.vue**: 带标签的输入行组件
- **SelectControl.vue**: 选择控件组件
- **ValueInputControl.vue**: 值输入控件组件
- **SimpleInputControl.vue**: 简单输入控件组件
- **RoiOverlay.vue**: ROI覆盖层组件
- **RoiToolsPanel.vue**: ROI工具面板组件
- **RoiControlHeader.vue**: ROI控制头部组件
- **FocusThumbnails.vue**: 对焦缩略图组件

## 状态管理

使用Pinia进行状态管理，分为以下几个store：

- **camera.js**: 管理相机连接和相机属性的状态
- **focus.js**: 管理对焦相关的状态
- **calibration.js**: 管理标定相关的状态
- **axis.js**: 管理轴相关的状态
- **roi.js**: 管理ROI相关的状态