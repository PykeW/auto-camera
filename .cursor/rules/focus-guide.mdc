---
description: 相机自动对焦功能的操作流程和实现原理
globs: 
alwaysApply: false
---
---
description: 相机自动对焦功能的操作流程和实现原理
globs: src/js/controllers/FocusController.js
alwaysApply: false
---
# 相机自动对焦功能操作指南

本文档提供相机自动对焦功能的详细操作流程和实现原理。

## 1. 对焦 ROI 操作流程

自动对焦前需要先设置感兴趣区域(ROI)，步骤如下：

### 1.1 对焦 ROI 绘制

1. **开始绘制**：
   - 点击对焦区域下方的"绘制"按钮
   - 系统显示通知："请在图像上按住鼠标左键并拖动以绘制矩形对焦区域"
   - 图像上鼠标光标变为十字形状，底部显示"绘制ROI"提示

2. **绘制矩形**：
   - 在图像上按住鼠标左键，拖动形成矩形区域
   - 松开鼠标左键完成绘制
   - 系统显示矩形选框和操作按钮组

3. **确认或修改**：
   - 点击"确认"按钮保存当前ROI区域
   - 点击"重绘"按钮重新开始绘制
   
4. **ROI 调整**：
   - 确认后，可通过"重绘"、"清除"按钮重新调整
   - 使用眼睛图标按钮可以临时隐藏/显示ROI区域

### 1.2 选择合适的ROI区域

对焦区域选择建议：
- 选择包含明显边缘或纹理的区域
- 区域大小应合适，不要过大或过小（至少5×5像素）
- 避开纯色或高光区域
- 避开可能移动的物体部分

## 2. 自动对焦操作

### 2.1 执行自动对焦

1. **开始对焦**：
   - 设置ROI区域后，点击"开始自动对焦"按钮
   - 系统显示"对焦中..."状态

2. **对焦过程**：
   - Z轴电机开始移动，系统按设定步长扫描不同Z位置
   - 状态栏显示当前Z值和清晰度评分
   - 对焦过程中图像会经历从模糊到清晰的变化

3. **对焦完成**：
   - 找到最佳位置后，Z轴停止在最佳清晰度位置
   - 状态显示"已对焦"，并提示清晰度值

### 2.2 停止对焦

任何时候可以通过以下方式停止对焦过程：
- 点击"停止"按钮立即中断对焦
- 等待对焦自动完成

## 3. 对焦模式和参数

### 3.1 对焦算法说明

系统采用以下算法评估图像清晰度：

```javascript
// 清晰度评估函数示例
function evaluateClarity(roiData) {
    // 基于梯度或标准差的清晰度算法
    let clarity = 0;
    
    // 计算ROI区域内像素值梯度的平方和
    for (let y = 1; y < height - 1; y++) {
        for (let x = 1; x < width - 1; x++) {
            const dx = imageData[y][x+1] - imageData[y][x-1];
            const dy = imageData[y+1][x] - imageData[y-1][x];
            clarity += dx*dx + dy*dy;
        }
    }
    
    return clarity / (width * height); // 归一化
}
```

### 3.2 调整对焦参数

对焦时可以调整以下参数：
- **Z轴范围**：通过轴配置设置最大、最小Z值
- **步长**：可调整扫描的精细程度
- **超时设置**：防止对焦过程无限进行

## 4. 对焦状态与反馈

系统提供多种对焦状态反馈：

| 状态 | 显示文本 | 含义 |
|------|----------|------|
| 空闲 | "空闲" | 等待开始对焦 |
| 对焦中 | "对焦中..." | 正在执行自动对焦 |
| 已对焦 | "已对焦" | 对焦成功完成 |
| 对焦失败 | "对焦失败" | 无法找到最佳清晰度位置 |
| 已停止 | "已停止" | 用户手动停止了对焦过程 |

## 5. 故障排除

### 5.1 常见问题

| 问题 | 可能原因 | 解决方法 |
|------|----------|---------|
| 无法开始对焦 | 相机未连接或轴未配置 | 检查连接和轴配置 |
| 对焦结果不理想 | ROI区域选择不当 | 选择有更多细节的区域 |
| 对焦过程很慢 | 步长设置过小或Z轴范围过大 | 调整步长和搜索范围 |
| 反复对焦到错误位置 | 图像中存在干扰或错误纹理 | 重新选择ROI区域 |

### 5.2 参考示例

以下是一个成功对焦的参考流程：

1. 连接相机，图像显示在主视图中
2. 配置Z轴，确保可以控制
3. 在图像上绘制包含清晰边缘的ROI
4. 点击"确认"保存ROI区域
5. 点击"开始自动对焦"
6. 等待系统扫描找到最佳Z位置
7. 对焦完成后，状态显示"已对焦"

## 6. 相关文件引用

- **UI控制器**: [src/js/controllers/UIController.js](mdc:src/js/controllers/UIController.js) - 管理ROI绘制界面
- **对焦控制器**: [src/js/controllers/FocusController.js](mdc:src/js/controllers/FocusController.js) - 控制自动对焦算法
- **对焦样式**: [src/css/features/focus-controls.css](mdc:src/css/features/focus-controls.css) - 定义对焦控制元素样式



