---
description: 相机自动标定功能的操作流程和实现原理
globs: 
alwaysApply: false
---
---
description: 相机自动标定功能的操作流程和实现原理
globs: src/js/controllers/CalibrationController.js
alwaysApply: false
---
# 相机自动标定功能操作指南

本文档提供相机自动标定功能的详细操作流程和实现原理。

## 1. 标定 ROI 操作流程

自动标定前需要先设置标定区域(ROI)，步骤如下：

### 1.1 标定 ROI 绘制

1. **开始绘制**：
   - 切换到标定板视图，点击标定区域下方的"绘制"按钮
   - 系统显示通知："请在图像上按住鼠标左键并拖动以绘制矩形标定区域"
   - 图像上鼠标光标变为十字形状，底部显示"绘制ROI"提示

2. **绘制矩形**：
   - 在标定板图像上按住鼠标左键，拖动形成矩形区域
   - 矩形应包含完整的标定图案（通常为棋盘格）
   - 松开鼠标左键完成绘制
   - 系统显示矩形选框和操作按钮组

3. **确认或修改**：
   - 点击"确认"按钮保存当前ROI区域
   - 点击"重绘"按钮重新开始绘制
   
4. **ROI 调整**：
   - 确认后，可通过"重绘"、"清除"按钮重新调整
   - 使用眼睛图标按钮可以临时隐藏/显示ROI区域

### 1.2 选择合适的标定区域

标定区域选择建议：
- 选择包含完整标定图案的区域（如棋盘格、圆点阵列等）
- 确保标定图案在视野中清晰可见
- 标定区域应包含足够多的特征点（如棋盘格交叉点）
- 避免包含运动物体或反光区域

## 2. 标定参数设置

### 2.1 标定板规格设置

1. **尺寸输入**：
   - 在标定面板中填入标定板的实际尺寸参数
   - 对于棋盘格，输入单个方格的实际尺寸（毫米）
   - 对于圆点阵列，输入相邻圆点中心之间的距离（毫米）

2. **标定类型选择**：
   - 根据实际使用的标定板类型选择相应选项
   - 支持的类型：棋盘格、圆点阵列、QR码角点

### 2.2 参数校验

系统会自动校验输入的参数：
- 尺寸必须为正数
- 数值必须在合理范围内（通常0.1-100mm）
- 输入无效会显示红色错误提示

## 3. 执行自动标定

### 3.1 标定过程

1. **准备工作**：
   - 确保标定板放置平稳
   - 检查照明条件，避免强反光
   - 确认标定板在ROI区域内清晰可见

2. **开始标定**：
   - 点击"开始标定"按钮
   - 系统显示"标定中..."状态通知

3. **标定计算**：
   ```javascript
   async startCalibration() {
       // 显示标定板
       this.switchToPatternView();
       
       // 分析图像，识别棋盘格特征
       await this.detectCalibrationPattern();
       
       // 计算像素与真实尺寸比例
       const knownSquareSizeMm = parseFloat(this.calibSquareSizeInput.value);
       this.calibrationRatio = this.DETECTED_SQUARE_SIZE_PX / knownSquareSizeMm;
       
       // 更新UI显示结果
       this.calibrationResultValue.textContent = `${this.calibrationRatio.toFixed(2)} px/mm`;
   }
   ```

4. **标定完成**：
   - 系统显示"标定成功"通知
   - 标定结果显示在标定面板
   - 显示计算出的像素/毫米比率

### 3.2 标定结果使用

标定结果可用于：
- 显示图像上的实际尺寸测量
- 计算物体实际大小
- 支持后续图像校正

## 4. 标定状态与反馈

系统提供多种标定状态反馈：

| 状态 | 显示文本 | 含义 |
|------|----------|------|
| 空闲 | "未标定" | 等待开始标定 |
| 标定中 | "标定中..." | 正在执行标定计算 |
| 已标定 | "已标定" | 标定成功完成 |
| 标定失败 | "标定失败" | 无法成功计算标定参数 |

## 5. 标定验证

### 5.1 验证流程

标定完成后，可以通过以下步骤验证标定准确性：

1. **测量验证**：
   - 在图像上使用测量工具测量已知尺寸物体
   - 比较测量结果与实际尺寸
   - 误差应在可接受范围内（通常小于1%）

2. **视觉检查**：
   - 检查标定计算过程中识别到的特征点
   - 所有特征点应准确匹配标定图案的交叉点

### 5.2 标定质量评估

系统提供标定质量评估数据：
- **重投影误差**：标识标定的精确度
- **标定置信度**：基于特征点识别质量

## 6. 故障排除

### 6.1 常见问题

| 问题 | 可能原因 | 解决方法 |
|------|----------|---------|
| 无法识别标定图案 | 照明不足或反光 | 调整照明条件 |
| 标定结果不准确 | 标定板放置不平 | 确保标定板放置平整 |
| 特征点检测不完整 | ROI区域选择不当 | 重新选择合适的ROI |
| 输入参数错误 | 标定板实际尺寸输入错误 | 重新测量并输入正确尺寸 |

### 6.2 参考示例

以下是一个成功标定的参考流程：

1. 放置标定板，确保视野中显示清晰
2. 切换到标定板视图
3. 绘制包含完整标定图案的ROI区域
4. 输入标定板实际尺寸参数（如方格尺寸10mm）
5. 点击"开始标定"
6. 等待系统计算完成
7. 标定完成后，查看计算结果（如35.6像素/毫米）
8. 使用测量工具验证标定准确性

## 7. 相关文件引用

- **UI控制器**: [src/js/controllers/UIController.js](mdc:src/js/controllers/UIController.js) - 管理标定ROI绘制界面
- **标定控制器**: [src/js/controllers/CalibrationController.js](mdc:src/js/controllers/CalibrationController.js) - 控制自动标定算法
- **标定样式**: [src/css/features/calibration.css](mdc:src/css/features/calibration.css) - 定义标定控制元素样式



