---
description: 相机标定规则
globs: 
alwaysApply: false
---
# 相机标定规则

## 标定流程

### 1. Mark点识别与居中
- 识别当前图像中的Mark点位置
  * 模拟实现：随机生成一个点位(x, y)，范围在图像中心±100像素
  * 返回点位信息：`{ x: number, y: number, confidence: number }`
- 通过PLC控制相机XY轴移动，使Mark点位于图像中心
  * 模拟实现：计算当前点到中心点的偏移，分解为X和Y轴的移动量
  * 移动过程：每次移动最多10mm，分步移动到目标位置
  * 移动完成后重新检测点位，直到偏差小于0.01mm
- 记录当前Mark点的像素坐标和PLC坐标作为基准点
  * 模拟数据：`{ pixel: {x, y}, plc: {x, y} }`

### 2. 标定矩阵生成
- 支持的矩阵格式：3×3、5×5、9×9
- 以基准点为中心生成标定点位矩阵
- 根据输入的偏移量计算每个点的PLC坐标
- 矩阵示例（5×5，偏移量10mm）：
  ```
  (-20,-20) (-10,-20) (0,-20) (10,-20) (20,-20)
  (-20,-10) (-10,-10) (0,-10) (10,-10) (20,-10)
  (-20,0)   (-10,0)   (0,0)   (10,0)   (20,0)
  (-20,10)  (-10,10)  (0,10)  (10,10)  (20,10)
  (-20,20)  (-10,20)  (0,20)  (10,20)  (20,20)
  ```

### 3. 数据采集
- 相机按顺序移动到每个标定点位
  * 模拟实现：每次移动延时500ms
  * 移动精度：±0.001mm
- 在每个点位进行拍照
  * 模拟实现：生成带有Mark点的模拟图像
  * Mark点位置：在目标位置±2像素范围内随机偏移
- 图片命名规则：`calib_x{plc_x}_y{plc_y}.jpg`
- 图片保存路径：`calibration/matrix_{size}/{timestamp}/`

### 4. Mark点检测
- 对每张标定图片进行Mark点检测
  * 模拟实现：在实际位置±2像素范围内生成检测结果
  * 检测置信度：0.85-0.99之间随机值
- 记录每个点位的数据：
  ```json
  {
    "plc": { "x": number, "y": number },
    "pixel": { "x": number, "y": number },
    "confidence": number,
    "timestamp": string
  }
  ```

### 5. 标定计算
- 使用采集的对应点数据计算：
  * 相机内参矩阵
  * 畸变系数
  * 相机与PLC坐标系转换矩阵
- 模拟实现：
  * 生成随机的内参矩阵
  * 生成合理范围内的畸变系数
  * 计算实际坐标与像素坐标的映射关系

## 模拟数据格式

### 1. 标定配置
```json
{
  "matrix_size": "5x5",
  "offset_mm": 10.0,
  "mark_size_mm": 2.0,
  "exposure_time": 5000,
  "gain": 1.0
}
```

### 2. 标定结果
```json
{
  "camera_matrix": [
    [fx, 0, cx],
    [0, fy, cy],
    [0, 0, 1]
  ],
  "distortion_coeffs": [k1, k2, p1, p2, k3],
  "transform_matrix": [
    [r11, r12, r13, tx],
    [r21, r22, r23, ty],
    [r31, r32, r33, tz]
  ],
  "pixel_size": {
    "x": number,
    "y": number,
    "unit": "um"
  },
  "accuracy": {
    "max_error": number,
    "avg_error": number,
    "unit": "mm"
  }
}
```

## 错误处理

### 1. Mark点检测失败
- 重试次数：最多3次
- 每次重试前调整：
  * 增加曝光时间（+1000us）
  * 提高增益（+0.5）
- 连续失败处理：
  * 记录失败点位信息
  * 继续下一个点位
  * 标定结束时重新采集失败点位

### 2. 运动错误
- 移动超时（>5秒）：
  * 记录错误日志
  * 重新尝试移动
- 到达软限位：
  * 记录当前位置
  * 调整目标位置到有效范围内
- 定位精度不足：
  * 记录实际位置偏差
  * 重新进行微调

### 3. 数据保存错误
- 磁盘空间不足：
  * 检查可用空间
  * 清理旧的标定数据
- 文件访问权限：
  * 使用临时目录
  * 提示用户修改权限

## 状态反馈

### 1. 进度显示
- 总体进度：已完成点位数/总点位数
- 当前状态：
  * 检测Mark点
  * 移动中
  * 拍照中
  * 数据处理中
  * 计算标定结果

### 2. 实时数据
- 当前点位坐标
- 检测置信度
- 位置偏差
- 剩余点位数

### 3. 结果显示
- 标定精度
- 像素当量
- 坐标系变换矩阵
- 重投影误差

## 数据格式

### 标定配置文件
```json
{
  "matrix_size": "5x5",
  "offset_mm": 10.0,
  "exposure_time": 5000,
  "gain": 1.0,
  "mark_type": "circle",
  "mark_size_mm": 2.0
}
```

### 标定结果文件
```json
{
  "timestamp": "YYYY-MM-DD_HH-MM-SS",
  "matrix_size": "5x5",
  "points_count": 25,
  "camera_matrix": [...],
  "distortion_coeffs": [...],
  "transform_matrix": [...],
  "average_error": 0.0,
  "max_error": 0.0
}
```

## 相关文件

- [app.py](mdc:app.py): 后端服务实现
- [script.js](mdc:script.js): 前端交互逻辑
- [index.html](mdc:index.html): 界面布局

## 注意事项

1. 标定环境要求
   - 确保光照均匀
   - 避免反光和遮挡
   - 保持相机参数稳定

2. 运动控制
   - 设置合适的运动速度和加速度
   - 确保每个点位稳定后再采集图像
   - 预留足够的缓冲距离

3. 数据管理
   - 定期备份标定数据
   - 记录标定环境参数
   - 维护标定历史记录



