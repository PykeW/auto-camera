---
description: 组件设计规范
globs: 
alwaysApply: false
---
# 组件设计规范

## 原子化设计原则
本项目采用原子化设计方法，将UI组件分解为最小可复用单元，以减少代码重复并保持样式一致性。

## 输入控件规范

### 基础输入框
- 所有基础输入框应使用统一的样式和结构
- 输入框应包含在`.position-display-container`容器内
- 标准高度为32px
- 边框颜色使用`var(--border-dark)`
- 背景色使用`var(--bg-medium)`
- 文字颜色使用`var(--text-light)`

### 数值调节控件
数值调节控件由三部分组成：减号按钮、输入框、加号按钮，应使用以下结构：
```html
<div class="axis-position-control">
  <button class="jog-btn minus">-</button>
  <div class="position-display-container">
    <input type="text" readonly>
    <span class="unit-display">单位</span>
  </div>
  <button class="jog-btn plus">+</button>
</div>
```

样式要点：
- 三个元素应无缝连接（无间隙）
- 只有左侧按钮左边和右侧按钮右边保留圆角
- 输入框不应有圆角
- 单位显示应位于输入框内右侧

### 下拉选择框
- 下拉框应使用`.compact-select`类或`.custom-select`类
- 下拉框应包含在`.position-display-container`容器内以保持与输入框宽度一致
- 下拉框不应有左边距（`margin-left: 0`）
- 项目中推荐使用`SelectDropdown`组件或`SelectControl`组件实现下拉功能，根据布局需求选择合适的组件

## 布局规范

### 控制面板项
- 使用`.control-item.side-by-side`类实现标签和控件的水平排列
- 标据剩余空间

### 示例参考
参考[FocusControl.vue](mdc:src/components/FocusPanel/FocusControl.vue)中的实现作为标准示例。

## 下拉选择控件 (SelectDropdown)

`SelectDropdown.vue` 是项目中统一的下拉选择框组件，用于创建具有标准布局和样式的下拉菜单。它替代了原有的特定用途组件（如 `ZAxisSelector.vue`），提供了更通用的下拉选择实现。此组件包含在标准布局容器中（使用`control-item side-by-side`类），适合表单布局。

### 文件位置
[SelectDropdown.vue](mdc:src/components/common/SelectDropdown.vue)

### Props
- `modelValue`: (String | Number, required) 绑定的选中值，用于 v-model。
- `options`: (Array, required) 选项列表，每个选项是一个对象，必须包含 `id` 和 `name` 字段，例如 `{ id: 'opt1', name: '选项1' }`。
- `selectId`: (String, default: 'dropdown-select') 下拉框的 ID 属性，用于标签关联。
- `label`: (String, required) 显示的标签文本。
- `placeholder`: (String, default: '') 可选的占位符文本，当未选择值时显示。
- `disabled`: (Boolean) 是否禁用下拉框。

### 结构
```html
<div class="control-item side-by-side">
  <label :for="selectId">{{ label }}:</label>
  <div class="position-display-container">
    <select class="compact-select" ...>
      <option v-if="placeholder" value="">{{ placeholder }}</option>
      <option v-for="option in options" :key="option.id" :value="option.id">
        {{ option.name }}
      </option>
    </select>
  </div>
</div>
```

### 样式特点
- 包含在 `.control-item.side-by-side` 容器内，保持与其他表单控件的布局一致
- 标签固定宽度为 80px，便于对齐
- 下拉框容器使用 `.position-display-container` 包装，占据剩余宽度
- 下拉框本身使用 `.compact-select` 类，具有右侧下拉箭头
- 重要：覆盖了全局样式中的 `margin-left: 8px` 以确保对齐一致

### 使用示例
```html
<SelectDropdown
  label="选择项目"
  v-model="selectedItemId"
  :options="itemOptions"
  selectId="item-select"
  placeholder="请选择"
/>
```

其中 `itemOptions` 格式为:
```js
[
  { id: '1', name: '选项1' },
  { id: '2', name: '选项2' },
  // ...
]
```

### 注意事项
- 对于已存在的 `ZAxisSelector` 组件，请考虑迁移到 `SelectDropdown` 组件
- 选项对象的属性应使用 `{ id, name }` 格式
- 组件内部应处理全局样式冲突问题，确保下拉框与其他表单元素对齐一致

## 下拉选择控件 (SelectControl)

`SelectControl.vue` 是项目中的独立下拉选择框组件，相比 `SelectDropdown` 更加轻量，不包含标准表单布局容器，适用于需要自定义布局的场景。

### 文件位置
[SelectControl.vue](mdc:src/components/common/SelectControl.vue)

### Props
- `modelValue`: (String | Number | Boolean | Object, required) 绑定的选中值。
- `options`: (Array, required) 选项列表，每个选项是一个对象，必须包含 `value` 和 `label` 字段，例如 `{ value: 'opt1', label: '选项1' }`。
- `placeholder`: (String, default: '请选择') 可选的占位符文本。
- `disabled`: (Boolean, default: false) 是否禁用下拉框。

### 结构
```html
<div class="select-control">
  <select 
    :value="modelValue" 
    @change="$emit('update:modelValue', $event.target.value)"
    :disabled="disabled"
    class="custom-select"
  >
    <option v-if="placeholder" value="">{{ placeholder }}</option>
    <option 
      v-for="option in options" 
      :key="option.value" 
      :value="option.value"
    >
      {{ option.label }}
    </option>
  </select>
</div>
```

### 样式特点
- 整体外观类似标准输入框，以便与其他表单元素对齐
- 使用 `.custom-select` 类，而非 `.compact-select`
- 自定义下拉箭头使用CSS背景图像实现
- 高度固定为32px，与其他控件保持一致
- 宽度默认为100%，可自适应父容器

### 使用示例
```html
<SelectControl 
  v-model="selectedOption"
  :options="[{ value: 'a', label: 'A' }, { value: 'b', label: 'B' }]"
  placeholder="选择一个字母"
/>
```

### 何时使用SelectControl vs SelectDropdown
- 使用`SelectControl`：
  - 需要自定义布局
  - 不需要带标签的标准表单行布局
  - 选项格式为`{value, label}`
- 使用`SelectDropdown`：
  - 需要标准的表单行布局
  - 需要与其他表单控件保持一致的外观
  - 选项格式为`{id, name}`

## 按钮组件规范 (ActionButton)

按钮组件 (`ActionButton.vue`) 用于执行操作。

### Props
- `text`: (String, required) 按钮上显示的文本。
- `type`: (String, default: 'default') 按钮类型，可选值为 'default', 'primary', 'secondary', 'danger'。
- `iconClass`: (String, default: '') 按钮图标的CSS类名。
- `disabled`: (Boolean, default: false) 是否禁用按钮。

### 样式
- **默认样式**: 
    - 边框: `1px solid var(--border-dark)`
    - 圆角: `var(--border-radius)`
    - 高度: `32px`
    - 内边距: `var(--spacing-small) var(--spacing-medium)`
- **Hover效果 (未禁用时)**:
    - 背景色: `#4a4a4a`
    - 边框颜色: `var(--border-medium)`
    - 阴影: `0 2px 4px rgba(0, 0, 0, .2)`
    - 垂直位移: `transform: translateY(-1px)`
- **Primary Button (`primary-button`)**:
    - 背景色: `var(--accent-blue)`
    - 边框颜色: `var(--accent-blue)`
    - 文字颜色: `#fff`
    - Hover背景色 (未禁用时): `#157acf`
- **Secondary Button (`secondary-button`)**:
    - 背景色: `#4a90e2`
    - 边框颜色: `#4a90e2`
    - 文字颜色: `#fff`
    - Hover背景色 (未禁用时): `#3a80d2`
- **Danger Button (`danger-button`)**:
    - 背景色: `var(--accent-red)`
    - 边框颜色: `var(--accent-red)`
    - 文字颜色: `#fff`
    - Hover背景色 (未禁用时): `#d9363e`
- **禁用状态 (`disabled`)**:
    - 背景色: `#4a4a4a`
    - 边框颜色: `var(--border-dark)`
    - 文字颜色: `var(--text-medium)`
    - 透明度: `.7`
    - 鼠标指针: `not-allowed`
- **图标**: 如果提供了 `iconClass`，图标会显示在文本左侧，并有 `var(--spacing-small)` 的右边距。

### 使用
```html
<ActionButton 
  text="点击我" 
  type="primary" 
  iconClass="fas fa-check" 
  @click="handleClick" 
/>
```

## 图标按钮 (IconButton)

图标按钮 (`IconButton.vue`) 用于仅显示图标的按钮，通常用于空间有限的场景。

### Props
- `iconClass`: (String, required) 图标的CSS类名。
- `tooltip`: (String, default: '') 鼠标悬停时显示的提示文本。
- `disabled`: (Boolean, default: false) 是否禁用按钮。
- `size`: (String, default: 'medium') 按钮尺寸，可选值为 'small', 'medium', 'large'。
- `variant`: (String, default: 'default') 按钮变体，可选值为 'default', 'outline', 'ghost'。

### 样式
- **基础样式**:
    - 背景色: `transparent`
    - 边框: `none`
    - 内边距: 根据 `size` 调整
    - 圆角: `var(--border-radius)`
- **Hover/Focus效果 (未禁用时)**:
    - 背景色: `var(--bg-light-hover)`
- **禁用状态 (`disabled`)**:
    - 透明度: `0.5`
    - G鼠标指针: `not-allowed`
- **变体 (variant)**:
    - `outline`: 带有边框 `1px solid var(--border-medium)`
    - `ghost`: 背景色在hover时更明显

### 使用
```html
<IconButton 
  iconClass="fas fa-cog" 
  tooltip="设置" 
  @click="openSettings" 
/>
```

## 带标签的输入行 (LabeledInputRow)

`LabeledInputRow.vue` 用于创建一个包含标签和任意内容（通常是输入控件）的行。

### Props
- `label`: (String, required) 显示的标签文本。

### 插槽
- `default`: 用于放置输入控件或其他内容。

### 结构与样式
- 标签和内容水平排列。
- 标签具有固定的右边距 `var(--spacing-medium)`。
- 整体垂直居中对齐。

### 使用
```html
<LabeledInputRow label="名称:">
  <input type="text" />
</LabeledInputRow>
```

## 值输入控件 (ValueInputControl)

`ValueInputControl.vue` 是一个复合组件，用于输入和调整数值，通常带有单位和步进按钮。

### Props
- `modelValue`: (Number, required) 绑定的数值。
- `label`: (String, default: '') 可选的标签。
- `unit`: (String, default: '') 数值的单位。
default: -Infinity) 最小值。
- `max`: (Number, defau。
- `step`: (Number, default: 1) 调整的步长。
- `readonly`: (Boolean, default: false) 输入框是否只读。
- `showButtons`: (Boolean, default: true) 是## 样式与行为
- 如果提供了 `label`，会使用 `LabeledInputRow` 进行布局。
- 包含一个显示当前值的输入框，右侧可显示单位。
- 如果 `showButtons` 为 true，输入框两侧会显示增减按钮。
- 增减按钮会根据 `step` 调整 `modelValue`，并受 `min` 和 `max` 限制。

### 使用
```html
<ValueInputControl 
  v-model="currentValue"
  label="温度:"
  unit="°C"
  :min="0"
  :max="100"
  :step="0.5"
/>
```

## 简单输入控件 (SimpleInputControl)

`SimpleInputControl.vue` 是一个基础的文本输入字段，可以配置类型、占位符等。

### Props
- `modelValue`: (String | Number, required) 绑定的值。
- `type`: (String, default: 'text') 输入框类型 (e.g., 'text', 'number', 'password')。
- `placeholder`: (String, default: '') 占位符文本。
- `readonly`: (Boolean, default: false) 是否只读。
- `disabled`: (Boolean, default: false) 是否禁用。

### 样式
- 遵循标准输入框样式。
- 高度: `32px` (通常与其他输入控件保持一致)。
- 边框: `1px solid var(--border-dark)`。

### 使用
```html
<SimpleInputControl 
  v-model="username"
  type="text"
  placeholder="请输入用户名"
/>
```

## 组件样式覆盖与对齐问题案例 (`ZAxisSelector.vue`)

### 问题描述
在将 "Z轴选择" 功能从 `FocusControl.vue` 抽离为独立的 `ZAxisSelector.vue` ([ZAxisSelector.vue](mdc:src/components/FocusPanel/ZAxisSelector.vue)) 组件后，出现了该组件的标签（"Z轴选择:"）与其在 `FocusControl.vue` ([FocusControl.vue](mdc:src/components/FocusPanel/FocusControl.vue)) 中的同级表单行（如"Z轴位置:"、"Z轴速度:"等）左侧对齐不一致的问题。具体表现为 `ZAxisSelector.vue` 组件看起来比其他行要窄一些，导致视觉上的缩进。

### 原因分析
经过排查，发现根本原因在于全局样式表 `styles.css` ([styles.css](mdc:src/assets/styles.css)) 中为 `.compact-select` 类定义了 `margin-left: 8px;`。当 `ZAxisSelector.vue` 组件内部的 `<select>` 元素应用了这个类时，这个左边距导致了其在父容器 `div.position-display-container` 内的偏移。

在 `FocusControl.vue` 中，有以下覆盖规则：
```css
.position-display-container .compact-select {
  margin-left: 0;
}
```

这条规则有效地抵消了全局样式的 `margin-left`。但当相关HTML结构被移入 `ZAxisSelector.vue` 后，由于Vue的样式作用域机制，这条位于 `FocusControl.vue` 中的 scoped 规则不再对 `ZAxisSelector.vue` 内部的元素生效。

为了解决这个问题，并在 `ZAxisSelector.vue` 组件内部保持正确的对齐，我们在 `ZAxisSelector.vue` 的 `<style scoped>` 中添加了针对 `.compact-select` 的样式覆盖规则：

```css
<!-- src/components/FocusPanel/ZAxisSelector.vue -->
<style scoped>
/* This rule is crucial to override the global margin-left: 8px on .compact-select */
.compact-select {
  margin-left: 0;
}
</style>
```

通过这个 scoped 样式，我们确保了 `ZAxisSelector.vue` 组件内部的 `.compact-select` 不会受到全局 `margin-left` 的影响，从而恢复了与其他表单行的正确对齐。

这个案例提醒我们：
1. **全局样式的潜在影响**：全局CSS类（如本例中的 `.compact-select`）可能会对组件内部元素的布局和外观产生非预期的副作用。
2. **组件抽取时的样式问题**：从一个大组件中抽取小组件时，需要确保所有必要的样式覆盖也一同迁移。
3. **样式作用域的理解**：Vue的scoped样式仅对当前组件有效，不会影响子组件，因此在组件重构时需要注意样式隔离问题。

当创建新的下拉组件时，应确保添加以下样式覆盖：
```css
.compact-select {
  margin-left: 0;
}
```



