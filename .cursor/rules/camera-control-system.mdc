---
description: 
globs: 
alwaysApply: true
---
# 工业相机控制系统 - 项目规则

## 项目结构
- `app.py`: 后端服务器，实现相机控制和数据模拟
- `index.html`: 前端界面主文件
- `script.js`: 前端JavaScript逻辑
- `styles.css`: 界面样式表
- `favicon.png`: 网站图标

## 核心功能
1. 相机连接管理
   - 自动连接功能
   - 固定序列号: "SN_Sim_1"
   - 连接/断开状态切换

2. 轴控制系统
   - XYZU四轴实时显示
   - 位置实时更新（100ms间隔）
   - 位置显示精度：保留3位小数
   - 轴范围限制：
     * X/Y轴：-100.0 到 100.0
     * Z轴：0.0 到 50.0
     * U轴：-180.0 到 180.0

3. 自动对焦功能
   - ROI区域选择
   - 粗对焦和精对焦过程
   - 实时清晰度计算

4. 图像采集功能
   - 连续采集
   - 单张拍照
   - 视频录制
   - 软件触发

5. 相机标定功能
   - Mark点检测与定位
     * 模拟检测延时：100-300ms
     * 检测置信度：0.85-0.99
     * 点位随机偏移：±2像素
   
   - 标定矩阵生成
     * 支持尺寸：3×3、5×5、9×9
     * 偏移量范围：1.0-50.0mm
     * 基准点：图像中心点
   
   - 运动控制
     * 移动延时：500ms/点
     * 位置精度：±0.001mm
     * 运动范围检查
   
   - 图像采集
     * 拍照延时：200ms
     * 图像分辨率：1920×1080
     * 自动曝光调整
   
   - 数据处理
     * 计算延时：1-2秒
     * 标定精度：≤0.01mm
     * 结果验证：重投影误差

## 模拟实现
1. 相机状态模拟
   - 连接延时：0.5秒
   - 状态更新间隔：100ms
   - 轴位置随机变化：±0.001范围内

2. 对焦过程模拟
   - 粗对焦步进：1.0mm
   - 精对焦步进：0.1mm
   - 清晰度计算：基于高斯分布

3. 标定过程模拟
   - Mark点检测
     * 随机生成点位：中心±100像素
     * 检测概率：95%成功率
     * 失败重试：最多3次
   
   - 相机移动
     * 分步移动：每步≤10mm
     * 到位延时：500ms
     * 位置抖动：±0.001mm
   
   - 图像生成
     * 基于实际位置生成Mark点
     * 添加随机噪声
     * 模拟光照变化
   
   - 数据存储
     * 自动创建目录结构
     * 图片命名规范化
     * 结果数据JSON格式

## API接口规范
1. 基础接口
   - `/connect`: POST，连接相机
   - `/disconnect`: POST，断开连接
   - `/status`: GET，获取状态
   - `/api/axes`: GET，获取轴配置

2. 功能接口
   - `/start_focus`: POST，开始对焦
   - `/stop_focus`: POST，停止对焦
   - `/start_capture`: POST，开始采集
   - `/stop_capture`: POST，停止采集
   - `/single_shot`: POST，单张拍照
   - `/start_recording`: POST，开始录制

3. 标定接口
   - `/api/calibration/detect_mark`: POST
     * 请求：`{ exposure: number, gain: number }`
     * 响应：`{ x: number, y: number, confidence: number }`
   
   - `/api/calibration/move_to`: POST
     * 请求：`{ x: number, y: number }`
     * 响应：`{ success: boolean, position: { x, y } }`
   
   - `/api/calibration/capture`: POST
     * 请求：`{ point_index: number }`
     * 响应：`{ image_path: string, timestamp: string }`
   
   - `/api/calibration/calculate`: POST
     * 请求：`{ matrix_size: string, points: Array }`
     * 响应：`{ matrix: Array, accuracy: Object }`

## 开发注意事项
1. 前端更新
   - 使用MCP工具可直接访问 http://127.0.0.1:5000/ 查看修改效果
   - 修改前端文件后无需重启后端服务

2. 代码规范
   - 保持代码结构清晰
   - 添加必要的注释
   - 遵循Python PEP8规范
   - 使用ES6+标准编写JavaScript代码

3. 错误处理
   - 添加适当的错误处理机制
   - 在UI中显示友好的错误提示
   - 记录关键操作日志

4. 性能优化
   - 避免频繁DOM操作
   - 使用防抖处理高频事件
   - 优化状态更新逻辑

## 测试要求
1. 功能测试
   - 验证所有按钮和控件的功能
   - 测试异常情况的处理
   - 确保状态更新的准确性

2. 性能测试
   - 检查CPU使用率
   - 监控内存占用
   - 验证响应时间

3. 标定测试
   - 检测准确性测试
     * Mark点检测率
     * 位置精度验证
     * 重复性测试
   
   - 运动控制测试
     * 定位精度
     * 运动平稳性
     * 超限保护
   
   - 数据处理测试
     * 计算性能
     * 内存占用
     * 存储效率


