{
  "name": "auto-camera",
  "version": "1.0.0",
  "description": "自动相机控制系统",
  "main": "script.js",
  "scripts": {
    "start": "http-server -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@playwright/test": "^1.42.1",
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "playwright": "^1.42.1"
  }
} 